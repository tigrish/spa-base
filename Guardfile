require './app'

# guard 'sprockets2', :sprockets => App.assets do
#   watch(%r{^assets/.+$})
#   watch('app.rb')
# end

guard 'livereload' do
  watch(%r{views/.+\.(haml)})
  watch(%r{^assets/.+$})
end
